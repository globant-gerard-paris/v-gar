CREATE TABLE `mygarage`.`additional_vehicle` (
  `id` INT NOT NULL,
  `make` VARCHAR(45) NOT NULL,
  `model` VARCHAR(45) NOT NULL,
  `year` INT NOT NULL,
  `color` VARCHAR(45) NOT NULL,
  `mileage` INT NOT NULL,
  PRIMARY KEY (`id`));
